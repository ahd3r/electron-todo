{
  "name": "todo",
  "version": "1.0.0",
  "description": "Todo for univer",
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --overwrite --asar=true --platform=win32 --arch=ia32 --icon=icon/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"ToDo\""
  },
  "author": "Ander and Slavik",
  "license": "ISC",
  "devDependencies": {
    "electron": "^4.1.1",
    "electron-packager": "^8.1.0"
  }
}
